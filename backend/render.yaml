services:
  - type: web
    name: westernpumps-backend
    rootDir: backend
    env: python
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        value: sqlite:///./westernpumps.db
      - key: JWT_SECRET
        value: replace-with-a-long-random-secret
      - key: CORS_ORIGINS
        value: "https://*.vercel.app"
      - key: DISABLE_AUTH
        value: "false"
      - key: AUTO_CREATE_TABLES
        value: "true"
      - key: ENFORCE_HTTPS
        value: "true"
